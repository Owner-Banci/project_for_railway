# railway-fastapi-client-server

FastAPI (API + static client) in one Docker container, Railway-ready.

## Local run
docker compose up --build

Open:
- http://localhost:8000
- http://localhost:8000/health
- http://localhost:8000/api/hello
